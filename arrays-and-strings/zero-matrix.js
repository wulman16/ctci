/**
 * Zero Matrix
 *
 * If an element in an MxN matrix is 0,
 * its entire row and column becomes zero
 *
 * Time complexity:
 * Space complexity:
 *
 * @param {Array<Array<number>>} matrix
 * @return {Array<Array<number>>}
 */
const zeroMatrix = matrix => {
  return matrix;
};

module.exports = zeroMatrix;
